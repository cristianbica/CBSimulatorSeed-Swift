# Uncomment this line to define a global platform for your project
platform :ios, '8.0'

target 'CBSimulatorSeed' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'AFNetworking'
  pod 'BlocksKit'
  pod 'SVProgressHUD'
  pod 'RHAddressBook'
  pod 'EDQueue'
  pod 'Inflections'
  pod 'MBFaker'

  # Pods for CBSimulatorSeed

  target 'CBSimulatorSeedTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
